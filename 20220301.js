// Now you have to write a function that takes an argument and returns the square of it.

// P: number
// R: number
// E:   (3) => 9
// PC: multiply number with itself and return

const square = (num) => {
  return num * num;
};
